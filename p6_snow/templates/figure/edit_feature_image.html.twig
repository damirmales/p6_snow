{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}


    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="p-2 updatePhoto">
                <div class="figEditTitle m-2"><h1>Modifier l'image de présentation<br> de {{ title }}</h1>
                </div>

                <div class=" p-2 updatePhoto">

                    {{ form_start(form) }}
                    {{ form_row(form.image_base) }}
                    {{ form_errors(form) }}

                    <button type="submit" class="btn btn-primary">Soumettre</button>
                    {{ form_end(form) }}
                </div>

            </div>
        </div>
    </div>

{% endblock %}

