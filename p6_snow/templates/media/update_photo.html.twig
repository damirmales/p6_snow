{% extends 'base.html.twig' %}

{% block title %}Mise à jour d'une photo{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col">
                <h1>Modifier la photo {{ title }}</h1>
                <img src="{{ asset('/uploads/figures/') }}{{ filename }}" alt="{{ filename }}"/>
                {{ form_start(form) }}

                {{ form_row(form.title) }}
                {{ form_row(form.file) }}
                <div class="row">
                    <button type="submit" class="btn btn-primary">Soumettre</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}